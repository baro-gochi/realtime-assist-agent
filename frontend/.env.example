# Backend API URL (터널 사용 시)
# localtunnel 사용:
# VITE_API_URL=https://my-backend.loca.lt
# ngrok 사용:
VITE_API_URL=https://your-subdomain.ngrok-free.app
VITE_WS_URL=wss://your-subdomain.ngrok-free.app/ws

# ============================================================
# Logging Configuration
# ============================================================
# 로그 레벨: DEBUG, INFO, WARN, ERROR, NONE
# - development: 기본 DEBUG (빌드 시 자동 설정)
# - production: 기본 WARN (빌드 시 자동 설정)
# - 명시적으로 설정하면 기본값 오버라이드
# VITE_LOG_LEVEL=DEBUG

# 백엔드 로그 전송 (개발용)
# - true: 프론트엔드 로그를 백엔드로 전송하여 파일로 저장
# - 저장 위치: backend/logs/frontend/frontend_YYYYMMDD.log
# - production에서는 자동 비활성화
# VITE_LOG_TO_BACKEND=true